# Copy to .env.local (gitignored) and fill in from the Supabase dashboard.
# Only the anon public key ever belongs here.
# The service role key must NEVER appear in this repo, in Vercel, or in the bundle.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
